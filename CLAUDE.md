# AI 工作指引

## 一、项目目的

本仓库是一个**用 Claude Code 协作长篇仙侠小说创作的工作台**。

- 当前重点是维护创作流程、资料结构与上下文质量
- 具体作品的名称、主角、主题、规模仍待作者锁定
- 已有世界观资料可视为基础素材，不自动等同于已定作品 canon

---

## 二、协作边界

1. **涉及创作决策时必须先问人类**：人物命运、情节走向、世界观定案、卷章标题等，由作者拍板。
2. **非创作决策可直接推进**：格式整理、文风校验、一致性检查、索引维护、资料同步等可直接执行。
3. **发现冲突必须停下**：章纲与设定、人物与时间线、伏笔与回收若发生冲突，不自行消解。
4. **术语必须复用仓库现有写法**：跨文件修改前先检索规范术语，再同步更新引用。
5. **不要把工作台占位符当成作品事实**：`待定` 代表作品规格未锁定，不得自行补完。

---

## 三、工作方式

- `CLAUDE.md` 只保留运行时最小约束；详细方法论见 `00_项目基础/统一方法论与工作流指导.md`
- 方法论总纲将要求分为“硬规则 / 默认模板 / 参考技法”三层；执行时先保硬规则，再按模板推进，最后做风格拔高
- `.claude/rules/` 承载常驻或按路径加载的规则
- `.claude/skills/` 是项目唯一自定义命令入口
- 除 `wenyan-writing` 外，项目 workflow skills 默认手动调用
- `.claude/settings.local.json` 在本仓库中被版本控制，这是项目特例，不代表 Claude Code 官方默认推荐方式

---

## 四、推荐流程

1. 用 `/daily-start` 恢复上下文与当前状态
2. 先读对应目录的 `INDEX.md`，再进入具体资料
3. 需要规划时使用 `design-*` / `create-character`
4. 需要创作或修订时使用 `write-chapter` / `edit-chapter`
5. 需要审查时使用 `review-*` / `check-consistency`
6. 会话结束前用 `/end-session` 收束记录

---

## 五、关键导航

| 用途 | 路径 |
|------|------|
| 人类维护手册 | `00_项目基础/使用指导书.md` |
| 方法论权威源 | `00_项目基础/统一方法论与工作流指导.md` |
| 世界观入口 | `01_世界观/INDEX.md` |
| 人物入口 | `02_人物/INDEX.md` |
| 大纲入口 | `03_大纲/INDEX.md` |
| 章纲入口 | `04_章纲/INDEX.md` |
| 正文入口 | `05_正文/INDEX.md` |
| 追踪入口 | `06_追踪/INDEX.md` |
| 灵感入口 | `08_灵感/INDEX.md` |
| 文风分层与 FAIL 级硬规则 | `.claude/rules/core-style.md` |
| 文件命名与索引规范 | `.claude/rules/file-conventions.md` |

---

## 六、当前项目状态

- 仓库定位已经明确：创作工作台
- 世界观基础资料已建档
- 已有少量基础角色档案；卷次大纲、章纲、正文仍处于未启动或占位状态
- 任何“作品级”信息若未在仓库中明确写定，默认视为未锁定
